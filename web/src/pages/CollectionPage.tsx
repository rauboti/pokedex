import { Heading, Stack } from '@chakra-ui/react'

/**
 * The collection landing page (`/`) — placeholder shell. The real list, register dialog and
 * filters land in US1/US2 (T019/T020/T022/T023).
 */
export const CollectionPage = () => (
  <Stack gap="4">
    <Heading size="lg">Collection</Heading>
  </Stack>
)
